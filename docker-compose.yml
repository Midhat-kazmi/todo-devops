version: '3'

services:
  backend:
    build: ./backend
    container_name: todo-backend
    ports:
      - "3001:3001"
    restart: always

  frontend:
    build: ./frontend
    container_name: todo-frontend
    ports:
      - "80:80"
    depends_on:
      - backend